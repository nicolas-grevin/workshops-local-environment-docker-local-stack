---
api:
  insecure: true
  dashboard: true

entryPoints:
  http:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedbydefault: false
    network: "local-stack-vnet"

log:
  level: "DEBUG"
